/**
 * 담당자2 owns GameEngine (loop, drop timer, and speed increase), GameState,
 * and ScoreCalculator. This package may call block, but must not import ui;
 * notify UI only through common.events.
 */
package com.team.tetris.game;