/**
 * 담당자2 owns GameEngine (loop and drop timer), GameState, GameAction,
 * scoring policies, and speed policies. This package may call block, but must not import ui;
 * notify UI only through common.events.
 */
package com.team.tetris.game;
