# Java 21 reads Windows launcher argument files using the native system charset.
# Match Gradle's generated worker classpath file to it for Korean checkout paths.
# Java source files and persisted game data explicitly use UTF-8.
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=384m -Dfile.encoding=COMPAT
